# Optional production fallbacks for each fixed-mode agent. A ?agent= URL sets
# the origin for the currently selected mode and is persisted in this browser.
VITE_AGENT_ORIGIN=http://localhost:7860
VITE_SIM_AGENT_ORIGIN=http://localhost:7861
